'use client';

// Placeholder no-op error listener.
// During migration away from Firebase this component is intentionally a no-op.
// If you need centralized permission/error handling later, reintroduce an
// emitter and wire it here.
export function FirebaseErrorListener() {
  return null;
}

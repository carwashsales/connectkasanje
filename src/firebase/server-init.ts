// Server init stub during migration to Supabase
// Server init removed: keep a no-op placeholder
// Server init / firebase bridge removed — DB migration is in `db/migrations/01_init.sql`.
export {};

// Firebase config stub removed for security. If you need to keep the original
// firebaseConfig for rollback/dev, re-add it in a secure manner (e.g. env vars).
// Firebase config removed during migration. If you need to restore it, add
// the values securely via environment variables instead of committing them.
// Firebase configuration intentionally removed during the Supabase migration.
export {};
